"""Visualisation: route plots, Gantt timelines, animations.

`mpl` produces static figures suitable for the thesis PDF and a FuncAnimation
exportable to MP4/GIF. `plotly` produces interactive HTML figures with hover
and a time slider for exploration of larger instances.

Both backends are optional. Install the `viz` extra:

    uv sync --extra viz
"""
